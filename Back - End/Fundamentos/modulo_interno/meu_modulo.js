module.exports = {
    soma(a, b) { //Não preciso declarar function, pois entende como uma função.
        console.log(a + b)
        
    }, //Para mais funções é necessário ","

    subtracao(a, b) { 
        console.log(a - b)
        
    }
}
